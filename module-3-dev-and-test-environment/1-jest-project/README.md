# Step by step to create project with Jest

1. npm init `npm init -y`
2. npm install library jest `npm install --save-dev jest`
3. basic running test `npm test`
4. running test with wath mode `npm test -- --watch --verbose`

--

## Add Library supertest

1. npm install library supertest `npm install --save-dev supertest`
